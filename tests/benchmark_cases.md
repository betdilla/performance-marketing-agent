# Benchmark cases

A: good CPA, weak downstream economics. Must not auto-SCALE.
B: CPA above target, strong cohort economics/payback. Must not auto-STOP.
C: rising frequency, falling CTR and conversion deterioration. Must identify creative-fatigue pattern.
D: stable upstream media, sharp downstream conversion break. Must investigate downstream rather than blame media.
E: excellent apparent metrics on 3 conversions. Must flag low confidence and avoid aggressive SCALE.
F: strong platform ROAS conflicts with backend. Must surface attribution discrepancy and require validation.

Cross-case requirements: distinguish observation from diagnosis, expose conflicting evidence, use LOW/MEDIUM/HIGH confidence, give concrete action and validation point, never invent missing data, use CAUSE NOT YET ESTABLISHED when appropriate, remain read-only.

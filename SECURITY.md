# Security

This repository is a read-only demonstration workflow.

Do not commit API keys, access tokens, passwords, cookies, client identifiers, confidential exports or real client data.

The current MVP does not require advertising-platform credentials and has no write-access integration.

If a credential is ever exposed, revoke it at the provider before removing it from Git history. Repository history should be treated as public.
